node_modules/
dist/
*.local
.DS_Store

# GTFS pipeline working files (raw downloads are not committed; /out is)
pipeline/raw/

# Synced copy of pipeline/out for the dev/build server (regenerated by sync:data)
public/data/
